name: note_taking
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.4
  google_fonts: ^3.0.1
  flutter_rich_text_editor: ^2.0.0
  cloud_firestore: ^4.0.2
  firebase_core: ^2.7.0
  cupertino_icons: ^1.0.5
flutter:
  uses-material-design: true
